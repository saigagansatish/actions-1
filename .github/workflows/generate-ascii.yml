name: Generate ASCII Artwork

on:  
  push

jobs:
  ascii-job:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repo
      uses: actions/checkout@v4

    - name: Executing Shell script
      run: ./ascii-script.sh
      